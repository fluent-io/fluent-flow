name: Test Minimal Reusable
on:
  workflow_call:
    inputs:
      test_input:
        required: true
        type: string
    secrets:
      TEST_SECRET:
        required: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test successful: ${{ inputs.test_input }}"